<%= form_for(@carpool) do |f| %>
  <% if @carpool.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@carpool.errors.count, "error") %> prohibited this carpool from being saved:</h2>

      <ul>
      <% @carpool.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

<div class="form-group">
  <%= f.label :description %>
    <%= f.text_field :description, class: "form-control"%>
  </div>

<div class="container">
    <div class="row">

 <%= f.label :starttime %><br />
 <%= time_field(:carpool, :starttime)%>    

<div class="container">
<div class="row">
 <%= f.label :endtime %><br />
   <%= time_field(:carpool, :endtime)%>    

<div class="container">
<div class="row">
<%= f.label :startdate %><br />
 <%= date_field(:carpool, :startdate)%>    

<div class="container">
<div class="row">
<%= f.label :enddate %><br />
 <%= date_field(:carpool, :enddate)%>    
<br >

  <div class="form-group">
    <%= f.submit class: "btn btn-primary" %>
  </div>
<% end %>

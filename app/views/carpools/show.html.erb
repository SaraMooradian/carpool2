	<p id="notice"><%= notice %></p>

<table class="table table-striped">
  <thead>
    <tr>
      <th>Description</th>
      <th>Start Time</th>
      <th>End Time</th>
         <th>Start Date</th>
      <th>End Date</th>
      <th colspan="5"></th>
    </tr>
  </thead>
<tbody>
<tr>
  <td><%= @carpool.description %></td>
<td>  <%= @carpool.starttime %></td>
<td>  <%= @carpool.endtime %></td>
<td>  <%= @carpool.startdate %></td>
 <td>  <%= @carpool.enddate %></td>
 </tr>
 </tbody>
 </table>
<% if @carpool.user == current_user %>
<%= link_to 'Edit', edit_carpool_path(@carpool) %> |

<% end %>
<%= link_to 'Back', carpools_path %>